Git commands which is used to push code :

touch .gitignore // file which you did not want in your github repository
git config --global user.name "Raffay"
git config --global user.email ar.raffaykhan1023@.gmail.com
git init // Add .git file in stageing area
git add. // all Files
git commit -m "first commit" 
git status // Check Status
git remote add origin [SSH-LINK] // 
To generrate SSh Key 
$ ssh-keygen -t rsa -b 4096 -C "ar.raffaykhan1023@gmail.com"
tail <path to id-sa.pub> // paste to your github profile
cat [SSH-Key]
git push origin master